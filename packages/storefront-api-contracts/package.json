{
  "name": "@arch/storefront-api-contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome check src"
  },
  "dependencies": {
    "@arch/storefront-core": "workspace:*",
    "@arch/storefront-validation": "workspace:*",
    "zod": "^4.3.6",
    "@hono/zod-openapi": "^1.2.2"
  }
}