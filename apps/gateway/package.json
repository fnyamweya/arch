{
  "name": "@arch/gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "dev": "wrangler dev --port 8787 --inspector-port 9230",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome check src",
    "test": "vitest run"
  },
  "dependencies": {
    "@arch/api-contracts": "workspace:*",
    "@arch/auth-contracts": "workspace:*",
    "@arch/cloudflare-bindings": "workspace:*",
    "@arch/observability": "workspace:*",
    "@arch/tenant-context": "workspace:*",
    "@hono/swagger-ui": "^0.6.1",
    "hono": "^4.7.0"
  }
}