{
  "name": "@arch/auth-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome check src",
    "test": "vitest run"
  },
  "dependencies": {
    "@arch/db-schema": "workspace:*",
    "@arch/cloudflare-bindings": "workspace:*",
    "@arch/shared-kernel": "workspace:*",
    "jose": "^5.9.6",
    "svix": "^1.59.0",
    "drizzle-orm": "^0.39.0",
    "hono": "^4.7.0"
  }
}
