import { SignInView } from "@arch/ui-kit";

export default function AdminSignInPage() {
  return (
    <SignInView
      logoText="Admin Portal"
      title="Manage your entire commerce platform from one place."
      description="Platform Administration"
    />
  );
}
