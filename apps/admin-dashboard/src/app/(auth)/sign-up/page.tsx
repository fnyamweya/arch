import { SignUpView } from "@arch/ui-kit";

export default function AdminSignUpPage() {
    return (
        <SignUpView
            title="Provision platform access with Better Auth"
            description="Admin operators use first-party identity with enterprise-grade credential and federation support."
            logoText="Admin Console"
        />
    );
}