{
  "name": "@arch/order-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "dev": "wrangler dev --port 8790 --inspector-port 9233",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome check src",
    "test": "vitest run"
  },
  "dependencies": {
    "@arch/cloudflare-bindings": "workspace:*",
    "@arch/db-schema": "workspace:*",
    "drizzle-orm": "^0.41.0",
    "hono": "^4.7.0"
  }
}