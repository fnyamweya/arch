import { SignUpView } from "@arch/ui-kit";

export default function VendorSignUpPage() {
    return (
        <SignUpView
            title="Launch vendor operations with first-party identity"
            description="Vendor teams sign up directly on the platform using Better Auth credentials and extensible identity plugins."
            logoText="Vendor Portal"
        />
    );
}