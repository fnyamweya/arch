export function VendorTopbar(): JSX.Element {
  return (
    <header className="h-14 border-b px-6 flex items-center justify-between">
      <span className="text-sm font-medium">Vendor Dashboard</span>
      <span className="text-xs text-neutral-500">tenant</span>
    </header>
  );
}
